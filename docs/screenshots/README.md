# Screenshots

Drop screenshots here and reference them from the main `README.md`. Suggested
shots (matching the README table):

- `dock-left.png` — the dock docked to the left edge with app + window icons.
- `transparent.png` — the dock fully transparent over a colorful desktop.
- `menu.png` — the right-click menu on the bar (Hide / Open config / Exit).

PNG is preferred. Keep them reasonably small (e.g. < 500 KB each).
